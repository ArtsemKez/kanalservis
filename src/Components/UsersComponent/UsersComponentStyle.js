import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    UserForTablet: {
        marginRight: 27,
        marginLeft: 27,
        maxWidth: '100%',
        display: 'flex',
        flexDirection: 'row',
        flexWrap: "wrap",
        justifyContent: "center"
    }
})